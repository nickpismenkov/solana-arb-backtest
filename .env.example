# Free key from https://flipsidecrypto.xyz (Settings → API Keys). Copy this file
# to .env and fill it in.
FLIPSIDE_API_KEY=

# Optional overrides (see src/config.ts for defaults)
# SIZE_USD=10000
# BUCKET_SECONDS=1
